{
  "name": "chat-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "start": "tsc && vite build && node server.js"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "handlebars": "^4.7.8"
  },
  "engines": {
    "node": ">=18"
  }
}
